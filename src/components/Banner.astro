---
interface Props {
  label: string
}

const { label } = Astro.props
---

{
  label && (
    <div class="mb-0 flex pt-20 flex-col items-center justify-center w-full">
      <h2 class="mb-8 text-5xl sm:text-8xl font-light text-center">{label}</h2>
    </div>
  )
}
